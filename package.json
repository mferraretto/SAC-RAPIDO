{
  "name": "sac-rapido",
  "version": "1.0.0",
  "description": "SAC – Central de Perguntas & Respostas (Firebase) como aplicativo desktop",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . SAC-RAPIDO --platform=win32 --arch=x64 --out=dist --overwrite",
    "package:linux": "electron-packager . SAC-RAPIDO --platform=linux --arch=x64 --out=dist --overwrite",
    "package:mac": "electron-packager . SAC-RAPIDO --platform=darwin --arch=arm64 --out=dist --overwrite"
  },
  "keywords": [
    "electron",
    "firebase",
    "faq"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^30.0.7",
    "electron-packager": "^17.1.2"
  }
}
